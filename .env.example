# ============================================================
# Clara AI Assistant — Environment Configuration
# Copy this file to .env and fill in your values.
# ============================================================

# --- Ollama (GPU VM) ---
# Clara and Ollama run on SEPARATE VMs.
# Point this to the IP of your Ollama/GPU VM.
OLLAMA_BASE_URL=http://<GPU_VM_IP>:11434
OLLAMA_MODEL=huihui_ai/qwen3-abliterated:14b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# --- Web server ---
# Bind to 0.0.0.0 so your main PC can reach Clara
HOST=0.0.0.0
PORT=8080

# --- Authentication ---
# Generate a strong password and a random 64-char hex secret:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
WEB_PASSWORD=change_me_strong_password
JWT_SECRET=generate_a_64_char_hex_string_here

# --- Storage paths ---
# Default: /opt/clara/data/ (SSD on Clara VM — no HDD needed)
# Override here only if you mount external storage:
# GENERATED_IMAGES_DIR=/mnt/storage/clara/images
# GENERATED_AUDIO_DIR=/mnt/storage/clara/audio
# UPLOAD_DIR=/mnt/storage/clara/uploads
# LOG_DIR=/mnt/storage/clara/logs
# DB_PATH=/opt/clara/data/clara.db

# --- Stable Diffusion Forge (optional, runs on GPU VM) ---
# SD Forge runs on the same GPU VM as Ollama.
# Set SD_API_URL to the GPU VM IP.
# SD_ENABLED=false
# SD_API_URL=http://<GPU_VM_IP>:7860
# SD_MODEL=sd_xl_base_1.0
# SD_HR_UPSCALER=R-ESRGAN 4x+

# --- n8n workflow automation (optional) ---
# Requires the n8n LXC container (see deploy/setup_n8n_lxc.sh).
# 1. Run the setup script on your Proxmox host
# 2. Open http://192.168.178.130:5678 and create an owner account
# 3. Settings → n8n API → Create API key → paste below
# N8N_ENABLED=true
# N8N_BASE_URL=http://192.168.178.130:5678
# N8N_API_KEY=n8n_api_...

# --- Discord bot (optional) ---
# Leave blank to disable the bot entirely.
DISCORD_BOT_TOKEN=
DISCORD_OWNER_ID=

# --- Google Calendar (optional) ---
GOOGLE_CALENDAR_ID=primary

# --- Misc ---
TTS_VOICE=de-DE-KatjaNeural
MAX_CONVERSATION_HISTORY=20
# Comma-separated list of allowed directories for the file manager skill.
# Use * for full access (default):
ALLOWED_DIRECTORIES=*
